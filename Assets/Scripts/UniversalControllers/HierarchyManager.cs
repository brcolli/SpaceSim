using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/// <summary>
/// Controls the universal hierarchy, allowing for branching paths
/// of systems. Uses a dictionary of storing parent systems to their children systems.
/// </summary>

public class HierarchyManager : MonoBehaviour {

	// Use this for initialization
	void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
